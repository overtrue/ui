# overtrue/ui design system

overtrue/ui 是用于 admin、dashboard 和 console 的界面工具包。产品品牌是 overtrue/ui；完整业务示例使用 **Acme Studio** 作为工作空间名称。示例人物、客户记录、邮箱、金额和操作均用于演示，不代表真实业务活动。

## 设计目标

密集但不拥挤，清楚但不生硬。保留成熟后台界面的阅读效率：紧凑的控件、稳定的表格行高、清晰的标题层级、克制的状态色。业务页通过内容关系和页面组合形成自己的视觉语言，不依赖原始模板的图片、人物或宣传文案。

## 两个界面层

官网与 Registry 使用 shadcn/ui 的语义变量、Tailwind 工具类和独立组件。完整业务示例使用 `.overtrue-workspace` 作用域、`pn-` 工具类和 `scene-` 页面组合。两者共享视觉原则，但 Registry 安装不需要引入工作空间样式或示例数据。

官网以长期阅读为目标：系统字体、克制的蓝灰色、细边框和稳定留白。首屏优先说明产品并露出真实预览，不使用高对比装饰背景抢占注意力。手机缩小展示标题，组件橱窗允许横向浏览，正文和文档保持自然纵向阅读。

产品路径完整连接展示、源码、安装、组合与排错。指南独立成页，提供当前导航状态和前后篇入口；组件详情可直接进入组合指南。FAQ 明确组件、Block、业务示例与后端服务的边界，不以页面数量暗示所有示例都能单独安装。

| 内容 | 位置 |
| --- | --- |
| 官网 | `src/site/` |
| 可安装组件 | `src/registry/overtrue/` |
| 业务页面组合 | `src/components/overtrue/scenes/` |
| 通用示例适配器 | `src/components/overtrue/workspace/` |
| 示例路由 | `src/pages/workspace/` |
| 统一示例数据 | `src/data/workspace/studio.ts` |
| 业务组合样式 | `src/styles/scenes.css` |

## 色彩

使用语义变量区分页面、表面、边框和文本。工作空间新增页面从现有主题读取 `--pn-body-color`、`--pn-secondary`、`--pn-bg-surface` 和 `--pn-border-color`，不要把浅色背景直接写进所有业务卡片。

| 用途 | 默认值 / 原则 |
| --- | --- |
| 主操作 | `#4263c7`，用于主要按钮、选中态和重点数据 |
| 正文 | 深灰蓝，以主题正文变量为准 |
| 次要信息 | 中性灰，不能替代必要标签 |
| 成功 | 低饱和绿色，配合文字状态 |
| 待处理 | 暖琥珀色，配合文字状态 |
| 异常 | 克制的红色，说明问题与下一步 |
| 图表 | 浅色系列与单个强调系列，避免整屏高饱和色 |

状态不只依赖颜色。Pill 使用文字、圆点与浅色底；表格重要状态始终保留可读文本。

## 字体与密度

采用系统无衬线字体栈。普通业务页标题约 25px、字重 650、轻微负字距；卡片标题 14–17px；正文 12–14px；表格与标签 11–12px。10px 文字只用于辅助元信息，不承载操作所需的唯一信息。

指标使用 29px 左右的数字。营销页和编辑文章允许更大的标题，但不把这种密度带进操作表格。

## 间距与容器

- 页面起始间距约 32px，手机约 22px。
- 卡片间距 24px，手机 20px。
- 卡片内边距 22px，手机 18px。
- 常用圆角 5px，卡片 8px。
- 默认桌面侧栏导航；布局示例可展示横向、折叠、流式、右侧和 RTL 变体。
- 主内容采用约 2:1 的主栏与辅助栏。不要让窄侧栏承载复杂表格。

## 页面组合

首页：横向指标组、收入趋势、项目进度表、最近活动和下次会议。

项目：项目封面卡、可切换状态的看板、紧凑工作队列和可调整顺序的优先级列表。项目名称、负责人和交付日期必须来自同一套数据。

客户：客户记录、负责人、合同金额和关系状态。示例客户使用中性行业描述，不暗示第三方企业与产品存在合作。

沟通：收件箱采用列表与内容双栏；聊天采用频道与消息区域；邮件模板保持独立阅读宽度。保存草稿与真实发送必须明确区分。

资产：摄影、设计封面、初始头像和 SVG 插图分别承担不同职责。网格、瀑布流与大图预览共享素材数据；下载使用实际文件。

账单：列表按状态筛选，发票使用可打印纸张布局，价格展示清楚说明计费周期。示例操作不产生真实付款。

设置与认证：表单标签常驻，必填项使用浏览器校验，提交后给出明确结果。认证示例使用原创工作空间插图或场景照片，不保存凭据。

通用组件：按钮、标签、菜单、输入、折叠面板等保持通用；示例文案围绕项目、团队、交付和权限，避免无意义占位段落。

## 图标与素材

图标继续使用 **Tabler Icons**（`@tabler/icons-react`），常用尺寸 16、20、24px，线宽 1.5–2。不得为了去品牌化改掉依赖名称或删除许可证。

`public/assets/overtrue/` 包含三张生成的场景照片、十二个原创几何项目封面、六种原创线性插图和团队初始头像。场景照片用于项目、工作空间与编辑内容；插图用于引导、空状态、访问和搜索。不要把整页截图作为实现。

通用旗帜、支付标识与集成品牌图标可保留，因为它们表达实际通用概念。第三方样式、库和资产的必要署名保留在专门的 notices 文件中。

## 交互与响应式

交互应完成用户看得见的动作：搜索改变结果，筛选改变范围，任务状态改变看板，表单显示明确结果，导出生成实际 CSV。演示操作不能宣称已经发送邮件或扣款。

桌面多栏在手机上降为单栏；指标可保留双栏；长表格在自己的容器内横向滚动，不撑宽整页。收件箱和频道导航在窄屏重新堆叠。资产预览支持 Escape 关闭、焦点约束和返回触发控件。

## 验证

构建和 Registry 验证是基础检查。每次改动业务组合后，还需要在真实浏览器检查 1440px 与 390px 布局、深色模式、图像加载、导航和主要交互。页面清单是覆盖范围的依据，不以首页检查代替全站检查。

## 可复用组合与 block

重复的展示模式发布为组件：`FeatureCard`（背景图或角落装饰）、`MediaCard`（上下或左右图片布局）、`DetailList`、`ResourceProgress`、`UptimeHistory`、`MetricGroup`。完整区域发布为 block：`ProjectPortfolio` 组合项目卡片与搜索，`ServiceStatus` 组合状态与历史。它们已用于工作空间页面，官网预览与 Registry 分发也直接使用相同源码。

优先使用 children、命名子组件和操作槽位，避免通过多个布尔参数拼出不同页面。业务数据、图片地址、路由和数据请求由应用提供；不将 Acme Studio 的示例数据放进通用组件。具体 API 和抽取边界见 `docs/registry/compositions.md`。

数据展示参考 dashboardcn 的层次划分，使用 overtrue/ui 自己的实现与视觉规范：`Sparkline` 表达紧凑趋势，`RankedList` 比较来源，`ConversionFunnel` 解释阶段转化，`AnalyticsOverview` 将这些组件组合为带周期选择与导出的完整报告，并复用于 Dashboard 的 Analytics 视图。

指标的变化方向与业务含义分开：延迟下降可以是正向变化。颜色之外保留方向、数值和可访问文字。对比曲线使用虚线；刻度简写与悬浮精确值分别格式化。排名条的长度相对最大值，百分比相对总和，必须明确分母。转化率同时展示相对首阶段与上一阶段的比例；无数据或分母为零时显示缺失状态，不生成伪造比例。分析 block 按容器宽度重排，嵌入窄主栏时也要降为单栏。
